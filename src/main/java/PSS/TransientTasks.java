package PSS;

public class TransientTasks extends Tasks {

    public TransientTasks(String name, String type, Float startTime,
                          Float duration, int startDate)
    {
        super(name, type, startTime, duration, startDate);
    }
}

